# 마케터로 살아남기 v9 — 선택지/상황 상호작용 안정화 전달서

## 적용 목적
v8.2는 카드 수와 랜덤성을 늘렸지만, 사용자 경험상 한 상황 안에서 선택 카드끼리 맥락이 맞지 않는 문제가 발생했다. v9는 랜덤성을 완전히 제거하지 않으면서도, 한 화면에 제시되는 3장의 카드가 같은 상황 안에서 자연스럽게 읽히도록 `choiceSets` 방식을 도입한다.

## 교체 파일
기존 파일 중 아래만 교체한다.

- `app.js` → v9 `app.js`
- `styles.css` → v9 `styles.css`
- `marketer_survival_story_card_patch_v2.json` → v9 패치 JSON

`index.html`, `marketer_survival_v3_data.json`은 유지한다.

## 핵심 변경 1. 무작위 후보군 → 선택 세트(choiceSets)
기존 v8.2는 각 라운드의 후보군을 크게 늘린 뒤 가중치로 3장을 뽑았다. 이 방식은 다양성은 늘지만, 한 화면에서 `업무 범위 정리 / 구매 전환 / 대표 설득`처럼 단계가 섞이는 문제가 생길 수 있다.

v9는 각 라운드에 5개의 `choiceSets`를 둔다.

```json
{
  "id": "r07 소재 고치기",
  "label": "소재 고치기",
  "choices": [
    { "role": "정석", "cardIds": ["creative_boost", "message_cleanup"] },
    { "role": "카피", "cardIds": ["hooking_copy", "cta_tighten"] },
    { "role": "확인", "cardIds": ["split_test", "small_test"] }
  ]
}
```

게임은 먼저 현재 라운드에 맞는 choiceSet 하나를 고른 뒤, 각 role 안에서 카드 1장씩 뽑는다. 따라서 3장의 선택지가 서로 같은 상황 안에서 자연스럽게 연결된다.

## 핵심 변경 2. R1 레퍼런스 지옥 정리
R1은 조사/기준/정리 단계다. 따라서 아래 카드는 R1에서 제외했다.

- 감도 연출
- 브랜드 가드
- 후킹 카피
- 구매자 포착
- 전환 보강
- 대표 설득

R1에는 아래 계열만 나오도록 정리했다.

- 레퍼런스 수집
- 질문 먼저
- 요청서 받기
- 톤 정리
- 효율 추적
- 테스트 목표 설정
- 브리프 다시 쓰기
- 브랜드 기준 필터
- 소스 요청
- 작업 쪼개기

## 핵심 변경 3. 카드 계열별 컬러/아이콘 통일
선택 카드가 같은 빌드 계열이면 같은 컬러와 아이콘을 사용하도록 `categoryClass()`와 CSS를 추가했다.

| 계열 | 아이콘 | 컬러 성격 |
|---|---|---|
| 예산관리 | ₩ | 골드 |
| 퍼포먼스 | ↗ | 레드 |
| 콘텐츠 | ✎ | 블루 |
| 브랜드 | ◆ | 퍼플 |
| 사내정치 | ☷ | 그린 |
| 운영력 | ▣ | 그레이 |

## 핵심 변경 4. 최근 카드/선택 세트 반복 방지
- 최근 제시된 카드는 가중치 감소
- 최근 선택된 choiceSet은 가중치 감소
- 같은 category가 2장 이상 몰리지 않도록 제한

## 테스트 결과
`SIMULATION_REPORT_v9.md` 기준 200회 자동 시뮬레이션을 수행했다.

- R1 고유 조합 수: 29개
- 대부분 라운드: 27~40개 고유 조합
- R3/R5는 업무 성격상 후보군이 좁아 21~24개 수준이나, 맥락상 자연스러운 조합으로 제한함

## 적용 후 확인 포인트
1. R1에서 감도 연출/브랜드 가드/전환 카드가 나오지 않는지 확인
2. 같은 상황을 여러 번 플레이했을 때 선택 카드 조합이 달라지는지 확인
3. 선택 카드의 계열별 아이콘/컬러가 유지되는지 확인
4. 결과 대사가 카드 선택과 너무 동떨어지지 않는지 확인
