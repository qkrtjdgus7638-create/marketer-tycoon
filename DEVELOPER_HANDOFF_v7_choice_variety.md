# 마케터로 살아남기 v7 선택지 다양화/카드 확장 전달서

## 목적
현재 배포본은 고정 12라운드 구조 자체는 좋지만, 라운드별 카드 슬롯 후보군이 좁아서 매번 비슷한 3장이 뜨는 문제가 있다. 특히 R1 `레퍼런스 지옥`처럼 후보군이 좁고 requiredCards가 있으면 선택지가 사실상 고정처럼 느껴진다.

v7의 목적은 아래와 같다.

1. 카드 수를 늘려 선택지 반복감을 줄인다.
2. 라운드별 후보군을 `핵심 / 맥락 / 상태보정` 3슬롯으로 재구성한다.
3. `requiredCards`를 제거해 특정 카드가 매번 고정 등장하지 않게 한다.
4. 현재 플레이 상태에 따라 세 번째 선택지가 달라지게 만든다.
5. 카드명은 유지하고, 설명은 상황에서 실제로 하는 행동처럼 다듬는다.

## 카드 수 변경

| 구분 | 수량 |
|---|---:|
| 기존 v3 기본 카드 | 20장 |
| v7 신규 카드 | 22장 |
| 최종 카드풀 | 42장 |

## 신규 카드 분류

### 예산관리
- 효율 추적
- 광고 중단
- 테스트 목표 설정
- 예산 속도 조절

### 퍼포먼스
- 전환 보강
- 클릭 회수
- 랜딩 확인
- 제안 테스트

### 콘텐츠
- 각도 정리
- 메시지 정리

### 브랜드
- 브랜드 가드
- 리뷰 확보
- 할인 절제
- 브랜드 기준 필터
- 표현 검수

### 사내정치
- 대표 설득
- 책임 분산
- 기대치 맞추기
- 중간 공유

### 운영력
- 대행사 호출
- 소스 요청
- 작업 쪼개기

## 선택지 생성 로직 수정안

### 기존
```text
정석 슬롯 1장
방어 슬롯 1장
정리 슬롯 1장
```

후보군이 좁으면 매번 거의 같은 카드가 나온다.

### v7
```text
핵심 슬롯 1장
맥락 슬롯 1장
상태보정 슬롯 1장
```

- 핵심: 해당 라운드를 가장 직접적으로 해결하는 카드
- 맥락: 상황과 자연스럽게 맞는 보조 해결 카드
- 상태보정: 현재 돈/성과/멘탈/신뢰/빌드 상태에 따라 달라지는 카드

## 추천 구현 규칙

1. `storyRound.cardSlots.핵심`에서 1장 뽑는다.
2. `storyRound.cardSlots.맥락`에서 1장 뽑는다.
3. `storyRound.cardSlots.상태보정`에서 1장 뽑는다.
4. 최근 2턴에 제시되었거나 선택된 카드는 가중치를 낮춘다.
5. 현재 상태가 위험하면 관련 방어 카드를 가중치로 올린다.
   - 돈 낮음 → 예산 방어/광고 중단/예산 속도 조절
   - 성과 낮음 → 구매자 포착/전환 보강/클릭 유도
   - 멘탈 낮음 → 일정 방어/작업 쪼개기/채널 분산
   - 신뢰 낮음 → 질문 먼저/중간 공유/보고서 방패
6. `badFits`와 현재 라운드 태그가 겹치면 가중치를 낮추거나 제외한다.
7. 같은 category 3장이 모두 나오면 마지막 카드를 다른 category로 교체한다.

## R1 레퍼런스 지옥 수정 예시

### 기존 문제
R1에서 `감도 연출`, `브랜드 가드`가 뜨면 조사 단계가 아니라 이미 시안 연출 단계처럼 보여 어색했다.

### v7 후보군

| 슬롯 | 후보 |
|---|---|
| 핵심 | 레퍼런스 수집 / 질문 먼저 / 각도 정리 |
| 맥락 | 요청서 받기 / 톤 정리 / 효율 추적 / 브랜드 기준 필터 / 테스트 목표 설정 |
| 상태보정 | 소스 요청 / 파일 정리 / 보고서 방패 / 기대치 맞추기 / 작업 쪼개기 / 소액 테스트 |

이제 R1은 조사/기준/정리/가벼운 준비 카드만 등장한다.

## 적용 파일

- `marketer_survival_choice_variety_patch_v7.json`
  - 신규 카드 22장
  - 기존 카드 설명 패치
  - 12라운드 storyRounds 재구성
  - 라운드별 contextDescriptions
  - 선택지 생성 로직 가이드

## 적용 방식

1. 기존 `marketer_survival_v3_data.json`은 유지한다.
2. 기존 `marketer_survival_story_card_patch_v2.json`을 v7 JSON으로 교체하거나, 파일명을 유지해야 하면 v7 파일명을 `marketer_survival_story_card_patch_v2.json`으로 바꿔 배포한다.
3. 가능하면 app.js의 `pickStoryCards`, `cardWeight`에 v7 로직을 반영한다.
4. 코드 적용 전이라도 v7 JSON만 적용하면 라운드별 후보군이 넓어져 반복감은 일부 줄어든다.
