# 마케터로 살아남기 v11 전달서 - EXP / 결과등급 시스템

## 적용 기준

기존 `marketer_survival_v3_data.json`은 유지합니다.
ZIP 안의 아래 파일만 교체합니다.

- `app.js`
- `styles.css`
- `index.html`
- `marketer_survival_story_card_patch_v2.json`

## 핵심 변경

### 1. 성과(score) 표기를 EXP로 변경

기존 내부 필드명 `score`는 유지합니다. 기존 로직과 데이터 호환을 위해 코드 내부에서는 계속 `score`를 사용하되, 화면 표시와 결과 문구에서는 EXP로 노출합니다.

- 기존: 성과
- 변경: EXP

EXP는 클리어 조건이자, 카드 결과 등급 확률을 긍정 방향으로 조금 보정하는 성장값입니다.

### 2. 카드 결과 등급 추가

카드 선택 결과에 아래 5단계 결과 등급을 추가했습니다.

- 대실패
- 실패
- 본전
- 성공
- 대성공

결과 등급은 회수 금액 배수와 EXP 획득량에 영향을 줍니다.

### 3. EXP 기반 확률 보정

EXP가 높아질수록 카드 결과 확률이 살짝 좋아집니다.

- EXP 5당 1단계 보정
- 최대 4단계 보정
- 대실패/실패 확률 소폭 감소
- 성공/대성공 확률 소폭 증가

주의: EXP가 높아져도 무조건 성공하지는 않습니다. 고위험 카드는 여전히 대실패/실패 가능성이 있습니다.

### 4. 카드 성격별 기본 확률

카드 성격에 따라 결과 등급 기본 확률이 다릅니다.

- 방어형: 대실패 낮음, 본전 높음, 대성공 낮음
- 안정형: 손실 낮음, 성공 보통, 대성공 낮음
- 중간형: 평균적인 변동성
- 고위험형: 대실패와 대성공이 모두 높음

### 5. 회수 금액 계산 변경

최종 회수 금액은 아래 구조로 계산됩니다.

```text
기본 회수 금액
× 빌드/상황/위험값 보정
× 결과 등급 배수
```

결과 등급 배수 예시:

- 대실패: 0배
- 실패: 0.25~0.6배
- 본전: 0.8~1.08배
- 성공: 1.2~1.6배
- 대성공: 2.0~2.8배

### 6. EXP 획득량 변경

기존 카드의 `score` 범위는 기본 EXP 범위로 사용합니다.
결과 등급에 따라 최종 EXP가 보정됩니다.

- 대실패: EXP 0
- 실패: EXP 0~1 수준
- 본전: 기본 EXP
- 성공: 기본 EXP + 1
- 대성공: 기본 EXP + 3

### 7. 결과 화면 변경

결과 화면에 결과 등급 pill이 추가됩니다.
예:

```text
대성공
팀장님: “이건 좀 터졌네요. 이 방향은 더 봐도 되겠습니다.”
쓴 예산 -50,000원
회수 금액 +172,000원
순손익 +122,000원
EXP +4
```

## 클리어 기준

패치 파일의 `stageDefaultsPatch`에서 아래 기준을 적용합니다.

- 일반 클리어: EXP 10 이상, 예산 300,000원 이상, 멘탈 1 이상, 신뢰 4 이상
- 우수 클리어: EXP 16 이상, 예산 430,000원 이상, 멘탈 3 이상, 신뢰 7 이상

## 구현 메모

- 내부 필드명 `score`는 유지합니다.
- UI 표기만 EXP로 변경합니다.
- 기존 카드/시너지 데이터와 충돌하지 않도록 기존 `score` 필드는 그대로 사용합니다.
