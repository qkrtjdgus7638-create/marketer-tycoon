# v8.2 선택지 다양성/맥락 자체 테스트 반영 변경사항

## 목적
고정 스토리 라운드는 유지하되, 매 라운드 선택 카드가 거의 고정처럼 느껴지는 문제를 줄이고, 카드가 현재 업무 단계와 어긋나는 문제를 보완한다.

## 핵심 변경

1. 카드 수 확장
- 기본 v3 카드 20장 유지
- 패치 신규 카드 30장 추가
- 병합 후 총 50장 기준

2. 카드 제시 로직 변경
- 기존: 라운드별 슬롯에서 1장씩 고정에 가깝게 제시
- 변경: 라운드별 후보군 전체에서 가중치로 3장 제시
- 핵심 카드 1장은 85% 확률로만 우선 선택
- 나머지는 후보군 전체에서 경쟁

3. 반복 방지
- 최근 1턴 선택 카드: 가중치 15% 수준으로 감소
- 최근 2턴 선택 카드: 가중치 35% 수준으로 감소
- 최근 3턴 선택 카드: 가중치 65% 수준으로 감소
- 직전 선택 카드와 같은 category는 감산
- 같은 category가 선택지 3장 중 3장 몰리지 않도록 최대 2장 제한

4. 상태 보정 카드 추가
- 예산 부족 → 예산 방어/광고 중단/효율 추적 계열 가중치 증가
- 성과 부족 → 성과 range가 높은 카드 가중치 증가
- 멘탈 부족 → 멘탈 회복 카드 가중치 증가
- 신뢰 부족 → 신뢰 회복 카드 가중치 증가
- 부족한 빌드 스탯을 보완하는 카드 가중치 증가

5. 자체 시뮬레이션 후 정리
- R1에서 `분할 실험`, `소액 테스트`, `회의 결론 뽑기`, `가격 저항 확인`, `회고 한 줄 남기기` 제외
- R2에서 `CTA 조이기`처럼 전환 단계에 가까운 카드 제외
- R3~R5에서 `회의 결론 뽑기` 제외
- R5에서 `채널 분산`, `광고 중단` 제외
- R6에서 `회고 한 줄 남기기` 제외

## 후보군 수
- R1: 14장
- R2: 16장
- R3: 12장
- R4: 15장
- R5: 12장
- R6: 14장
- R7: 15장
- R8: 17장
- R9: 16장
- R10: 16장
- R11: 17장
- R12: 18장

## 자체 테스트 결과
100회 자동 시뮬레이션 기준 라운드별 제시 카드 조합 고유 수가 대부분 70~90개 이상으로 확인됨.
자세한 내용은 `SIMULATION_REPORT_v8_2.md` 참고.

## 적용 파일
- `app.js` 교체
- `marketer_survival_story_card_patch_v2.json` 교체
- 기존 `index.html`, `styles.css`, `marketer_survival_v3_data.json`은 유지 가능
