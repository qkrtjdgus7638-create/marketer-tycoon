# 개발자 전달서: v8.2 선택지 다양성/맥락 자체 테스트 반영

## 적용 목적
현재 플레이에서 같은 상황마다 선택 카드가 거의 고정처럼 느껴지고, 일부 카드가 업무 단계와 어긋나는 문제가 있다. v8.2는 기존 구조를 크게 바꾸지 않고 다음 두 가지를 개선한다.

1. 선택지 경우의수 확대
2. 카드-상황 맥락 유지

## 교체 파일

기존 파일명 기준으로 아래 2개를 교체하면 된다.

```text
app.js
marketer_survival_story_card_patch_v2.json
```

유지 파일:

```text
index.html
styles.css
marketer_survival_v3_data.json
```

## 데이터 구조

`marketer_survival_story_card_patch_v2.json`는 v8.2 패치 파일이다.
주요 내용:

- 신규 카드 30장
- 라운드별 `cardSlots` 후보군 확장
- `requiredCards` 제거
- 기존 카드 설명/fits/badFits 보완
- 자체 테스트 후 어색한 초반 후보군 정리

## app.js 로직 변경 요약

### 기존 방식

```text
라운드 cardSlots의 각 슬롯에서 1장씩 선택
→ 슬롯 후보가 좁으면 선택지가 거의 고정처럼 보임
```

### 변경 방식

```text
1. 현재 storyRound의 모든 cardSlots 후보군을 합친다.
2. 핵심 슬롯 카드는 85% 확률로만 1장 우선 제시한다.
3. 나머지 카드는 후보군 전체에서 가중치로 뽑는다.
4. 최근 선택 카드, 같은 category 반복, badFits를 감산한다.
5. 부족한 돈/성과/멘탈/신뢰/빌드 스탯에 맞는 카드를 가산한다.
6. 같은 category가 3장 모두를 차지하지 않게 최대 2장으로 제한한다.
```

## 카드 적합 판정

기존에는 태그 교집합만으로 matched/missed가 갈렸다.
변경 후에는 다음 조건 중 하나면 상황에 맞는 카드로 본다.

```text
- 현재 storyRound.cardSlots 후보군에 포함된 카드
- card.fits와 상황 태그가 겹치는 카드
- card.tags와 상황 태그가 겹치는 카드
```

단, `badFits`가 현재 상황 태그와 겹치면 부적합 처리한다.

## 자체 테스트 결과

로컬에서 v3 기본 데이터 + v8.2 패치를 병합한 뒤 100회 자동 시뮬레이션을 돌렸다.

확인 사항:

- 누락 카드 ID 없음
- 병합 후 총 카드 수 50장
- 라운드별 후보군 최소 12장 이상
- 각 라운드 카드 조합 고유 수 대체로 70개 이상
- R1/R3/R5에서 업무 단계와 어긋나는 카드 후보 제거 완료

자세한 결과는 `SIMULATION_REPORT_v8_2.md` 참고.

## 주의
브라우저 실제 플레이 테스트는 배포 환경에서 다시 확인해야 한다. 다만 JS 문법 체크는 통과했다.
