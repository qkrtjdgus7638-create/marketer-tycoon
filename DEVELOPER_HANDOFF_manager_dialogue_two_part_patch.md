# 개발자 전달서 - 팀장 대사 2문장 구조 패치

## 적용 목적
현재 결과 화면의 팀장 대사가 너무 길고 설명적이라 게임 템포가 늘어지는 문제가 있습니다. 이번 패치는 팀장 대사를 더 짧고 자연스럽게 만들기 위한 수정입니다.

## 교체 방법
기존 `marketer_survival_v3_data.json`은 유지하고, ZIP 안의 아래 파일만 교체해주세요.

- `app.js`
- `styles.css`
- `index.html`
- `marketer_survival_story_card_patch_v2.json`

## 주요 변경

| 항목 | 내용 |
|---|---|
| 팀장 대사 구조 | `선택 인정 + 결과 해석` 2문장 구조로 변경 |
| 다음 방향 문장 | 결과 화면에서 제거 |
| 카드명 직접 삽입 | 제거하고 행동 표현으로 변환 |
| 조사 오류 | `초반 부스팅는` 같은 문장 방지 |
| 기존 결과 계산 | 유지 |
| 기존 EXP/결과등급/빌드 로직 | 유지 |

## 구현 메모
`app.js` 하단에 기존 팀장 대사 관련 함수를 덮어쓰는 방식으로 적용되어 있습니다.

- `composeManagerDialogueLine()`
- `cardActionPhrase()`
- `dialogueAcceptancePool()`
- `dialogueInterpretationPool()`

`dialogueNextPool()`은 파일 안에 남아 있지만 더 이상 호출하지 않습니다. 기존 구조를 크게 건드리지 않기 위해 삭제하지 않고 비활성화했습니다.

## 기대 효과
- 팀장 대사가 짧아져 결과 화면 템포가 좋아짐
- 카드명 직접 삽입으로 생기는 어색한 문장 감소
- 선택 결과를 짧게 해석하는 상사 피드백에 가까워짐
